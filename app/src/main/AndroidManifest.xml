<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homework1">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ReminderApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Homework1">
        <activity android:name=".MapActivity"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAmRhljclr5xLzZF3UddU55a-fYoETO3o4" />

        <activity android:name=".MessageEditDeleteActivity" />
        <activity android:name=".NewMessageActivity" />
        <activity android:name=".MenuHelpActivity" />
        <activity android:name=".LogHelpActivity" />
        <activity android:name=".MainHelpActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SignActivity" />
        <activity android:name=".LogActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Homework1.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>